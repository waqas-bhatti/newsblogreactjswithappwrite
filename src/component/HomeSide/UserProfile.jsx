import React, { useEffect } from "react";
import appwriteService from "./AppwriteService"; // Import the AppwriteService

const UserProfile = () => {
  return (
    <div>
     This is projfi
    </div>
  );
};

export default UserProfile;
